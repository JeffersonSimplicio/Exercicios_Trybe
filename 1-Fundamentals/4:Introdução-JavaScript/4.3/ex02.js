let word =  'tryber';

let sub = word.split('').reverse().join('') 
console.log(sub);

// .split('') separa a string caractere por caractere colocando-os em uma array
// .reverse() inverte a ordem dos caracteres de uma array
// .join('') junta os caracteres de um array em uma string, o que tiver entres as aspas sera colocando entre as palavras
